version: 2

jobs:
  test:
    docker:
      - image: circleci/node:latest-browsers

    steps:
      - checkout
      - run: npm i
      - run: npm run test

workflows:
  version: 2
  test:
    jobs:
      - test
